## 需求

1. 如何让claude或者codex cli控制另外一个终端，可视化的控制，对于这些coding agent，不都可以执行bash命令，这有什么区别吗？我想让agent可视化控制，
2. 如何才能让这些agent控制终端，同时做到互相通信
3. 同时codex cli不是开源的吗？我能不能修改源码，然后提一个pr，添加一个模式开关，执行bash命令的时候，是在另外一个终端可视化执行的
   1. 或者能不能写一个codex或者claudecode的插件来完成这个功能
