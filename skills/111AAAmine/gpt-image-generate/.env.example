# 与 run.sh 同级；复制为 .env 后填写
# cp .env.example .env

OPENAI_API_KEY=你的key填这里
OPENAI_BASE_URL=https://shell.wyzlab.ai/v1
OPENAI_MODEL=gpt-image-2

# 单次请求超时（秒）。生图接口慢时可调大，但过大会导致干等很久
# CURL_CONNECT_TIMEOUT=20
# CURL_MAX_TIME=180
