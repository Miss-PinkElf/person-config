# Resume 路径示例

场景：用户说“继续上次那个 devflow 任务”。

## 执行方式

1. 读取 `state.md`
2. 读取最新 `handoff`
3. 判断当前任务应继续轻量、重型还是 bug 路径
4. 如果历史状态过旧，先做一致性检查
5. 确认继续、回退或重新对齐

## 关键判断

- 如果历史记录与当前代码事实冲突，以当前事实为准
- 如需大改方向，回到 Align 或 Plan
- 仅当需要正式交接时才继续写新 handoff
