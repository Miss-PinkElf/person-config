# 决策日志（Decision Log）

## 2026-07-13：采用轻量路径（Light Path）

- 决策：按轻量路径推进。
- 原因：仅修改一份 JSON 配置（JSON Configuration），字段、模型范围与验收方式均已明确。
- 影响：实施前仍必须具备最小计划（Plan）与轻量任务（Light Tasks），但不创建完整的 OpenSpec 三件套。

## 2026-07-13：只调整现有模型，不新增 `gpt-5.5`

- 决策：不新增 `gpt-5.5` 条目。
- 原因：用户明确确认“无需新增”。
- 影响：本次仅处理目录中当前存在的五个模型。

## 2026-07-13：以用户提供数值更新上下文窗口（Context Window）

- 决策：`gpt-5.6-terra` 与 `gpt-5.6-sol` 写入 300,000；`gpt-5.4` 写入 1,000,000；`grok-4.5` 保持 500,000。
- 原因：用户明确提供该能力目录应使用的数值。
- 影响：同时同步 `context_window` 和 `max_context_window`，避免目录内两个窗口字段相互矛盾。

## 2026-07-13：计划完成后直接进入实施（Apply）

- 决策：完成轻量计划（Light Plan）与轻量任务（Light Tasks）后，不等待额外执行确认，直接按任务推进。
- 原因：用户明确指示“写完 plan 然后直接 apply”。
- 影响：仍保留外部文件写入授权和执行后验证两项环境与质量门禁。

## 2026-07-14：外部目录更新并经独立读取校验

- 决策：完成对外部模型目录（Model Catalog）的定点更新，并以独立 Python 进程验证。
- 证据：备份文件为 `~/.codex/cc-switch-model-catalog.json.bak-20260714T091900Z`；五个现有模型均具备 `text` 和 `image` 输入模态，图片详情支持字段均为 `true`；四个指定模型的窗口断言全部通过；未发现 `gpt-5.5`。
- 影响：CC Switch 不应再因本地能力声明而拒绝向当前五个目录模型附带图片。上游服务端是否实际接受图片仍由其响应决定。

## 2026-07-14：收尾并等待运行时验证

- 决策：本轮在配置结构验证通过后收尾，不继续推断或伪造上游图片请求结果。
- 原因：本次范围是修正本地模型目录（Model Catalog），尚未获得 CC Switch 的运行时测试证据。
- 影响：下次优先由用户重启或重新加载应用后发送图片；如仍失败，基于实际错误进入调试（Debugging）路径。
