# 示例：从 handoff 恢复

## 场景

用户说：“继续上次那个 skill 改造任务。”

## 动作

1. 找到对应 `.explore/<mission>/handoffs/index.md`
2. 读取最新 handoff
3. 参考 `resume-checklist.md` 核验当前状态
4. 必要时补读上一份 handoff 与 `spec/` artifact
5. 从 handoff 中的“立即下一步”继续

## 结果

AI 可以快速恢复到正确阶段，而不是重新猜测当前应该做什么。
