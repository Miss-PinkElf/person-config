# 受控第二文件管理器（Controlled Explorer）工作流（Workflow）

## 当前目标

基于 `zzz-prompt-debug/受控文件夹/prompt.md` 恢复并继续推进受控第二文件管理器（Controlled Explorer）VS Code 插件（VS Code Extension）的开发任务。

## 当前路径

- 路径：重型路径（Heavy Path）
- 原因：原始提示词包含既有能力、待观察问题、后续功能方向与插件代码位置不明等多项不确定因素，需要先完成对齐（Align）与计划（Plan），再决定是否进入正式规格（Spec）和实施（Apply）。

## 当前阶段

- 阶段：收尾（Close / Handoff）
- 当前不继续新增功能；下一轮从手动验证和反馈修复开始。

## 范围

- 范围内：梳理现有受控第二文件管理器（Controlled Explorer）需求、确认代码位置、确定本轮优先目标、形成计划与任务。
- 范围外：在未确认目标和计划前直接修改插件业务代码。

## 成功标准

1. 明确本轮要解决的是验证、修复、增强还是恢复代码位置。
2. 对齐本轮优先级与验收标准。
3. 将计划（Plan）落到本 mission 的 `plans/` 目录。
4. 进入实施前具备明确任务定义（Tasks）。

## 下一步

新对话恢复后，先读取 `state.md` 与 `checkpoints.md`，再按 `NEXT-SESSION-PROMPT-vscode-controlled-explorer.md` 安装/验证 VSIX。
