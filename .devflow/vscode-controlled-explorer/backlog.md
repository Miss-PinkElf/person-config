# 后续想法（Backlog）

- 在 VS Code Extension Host 中逐项手动验证 22 项能力，并把实际反馈转成 bug 或下一版任务。
- 考虑补充 `repository` 和 `LICENSE`，消除 `vsce` 打包警告，为发布到 Visual Studio Marketplace 做准备。
- 考虑补充 Extension Host 自动化测试（Extension Integration Tests），覆盖 Tree View（树视图）命令注册和菜单行为。
- 观察外部拖拽（External Drag and Drop）在不同 VS Code 版本、macOS / Windows / Linux 下的 MIME 类型差异。
- 根据实际使用反馈决定是否支持把新增入口直接添加到指定虚拟分组（Group）。
- 根据实际使用反馈决定是否同步 `controlled-explorer-config` skill 到 `.claude/skills` 或 `.codefuse/skills`。
