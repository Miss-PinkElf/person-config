# 延期项：配置项（Configuration Options）

## 延期内容

第一版不提供配置项（Configuration），包括：

- 是否追加尾部空格。
- 是否改变路径格式。
- 是否在无选区时采用其他行为。
- 是否改变默认发送目标。

## 延期原因

第一版目标是把核心路径做稳：选中代码行 -> 生成引用 -> 发送到当前活动终端（Active Terminal）且不自动执行。配置项会扩大测试矩阵和文档成本。

## 未来触发条件

当用户在实际使用后反馈需要可配置行为时，再进入新一轮 devflow 变更。
