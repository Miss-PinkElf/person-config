### 需求

1. 比如claude或者codex cli 调用subagent的时候，都是黑盒的，都是在后台的，我没有干预或者中断，有没有什么可以解决这个问题
2. 类似于claude code的agent team那样，有一个主agent，subagent是可视化的，我可以介入的，如图所示![alt text](images/image.png)
3. 我理想的状态是：
   1. 主agent可以像人一样输入提示词，输入slash命令，新开对话，清空上下文，我人也可以介入，其实就类似一个真人，在终端输入 codex，然后可以进行各种各样的操作，不过这个人变成了agent
   2. 或者能不能写一个skills，这个skills作用就是启动单独的cli回话代替subagent，如果主agent想知道subagent做了什么，可以通过写一个json或者共同维护某一个mission（你看一下devflow这个skills看看可不可行）

### 需求

1. 我想的是这个skills+cli
2. 同时这个skills，是为了指导如何使用cli，如何交接，我觉得里面应该内置一个专门用于交接的提示词或者子skills，或者参考devflow的交接方法，或者有一部分文件共用devflow的交接文件，你可以参考这个提示词了解，devflow的交接方法：`txt 现在上下文太长了，我新开一个对话在进行接下来的步骤，或者讨论没有讨论的问题,  对了，有些可能只做第一版的，有些可能后续明确延期的，记得在文档里面记录，你需要再回顾一下本次的对话，再次核对一下，同时现在阅读，根目录devflow-handoff.md收尾,把你没有收尾的文档更新一下,然后提交相关的代码（收尾哪个mission，就提交相关的文件），把你没有更新的文档记得更新,等会直接提交即可（只提交和本次对话相关的代码（不用精确到行，如何某一行或者某一段不是这次对话的，你可以询问我），如果出现不相关的可能是另外一个codex修改的，或者我之前修改的，默认不用提交config.ts，gitignore,tsconfig.json,.devflow/，mock-server），不用向我确认，先提交代码之后再收尾`
3. cli的作用是用来开终端，输入命令，输入提示词之类的的作用，同时为了节省上下文，文档方面可以采取滑动窗口的方式，有最新的，有归档的，文档

### 需求

1. 其实感觉不想要那么多分类，这个cli应该只有，这几种操作，知道 控制 的终端 发生了什么，然后把情况给codex ，codex做判断然后发送命令给，被控制的终端，进
   行接下来的操作，然后再把操作的结果反馈给codex，像人一样盯着这个终端，知道发生了什么，然后知道应该如何做，我想要的效果是这样的，那么基础的操作不就
   是，get_info,excu_bash,获取结果，进行操作
2. 现在我有哪些操作，或者可以从skills本身入手，修改skills的描述，来限定操作，同时输出结构化信息，这个同时需要结合文档辅助，比如现在不是已经有'/Users/
   yangyingtao/Documents/AAA-Code/person-code-config/.devflow/devflow-cli-worker/sessions'类似这个文件夹，我还是不太清楚，我现在的架构，其实这个等待时
   间应该是需要的，同时定时获取结果，也是需要的，但是不能太占用上下文，可以参考codex本身对于subagent的管理方式

### 需求

1. 对于working，就是编码的等待时间，最大是20分钟，同时是不是应该轮询状态？
   1. 比如每15s看一下或者别的时间间隔，如果有授权或者别的异常也可以提前知道，或者也可以知道subagent提前完成了工作，我发现你会一直等，一直等
2. 同时对于result.md在组装prompt的过程中没有明确这个文件写在哪里，导致找不到

### 需求

1. 等一下，其实最好是用户的原始的提示词，同时在组装提示词的时候，使用相对路径把result标注一下，要不然还是不知道，同时这个插件可以并行吗？多开好几个worker