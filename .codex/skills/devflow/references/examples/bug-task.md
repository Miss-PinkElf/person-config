# Bug 路径示例

场景：一个原本正常的功能开始在测试中随机失败，根因不明。

## 路径判断

判断为 bug 路径，因为：

- 现象是失败与异常
- 根因未知
- 不适合直接打补丁

## 执行方式

1. 进入 `superpowers-systematic-debugging`
2. 记录现象、复现方式、证据
3. 写 `bug-log.md`
4. 找到根因后实施修复
5. 做验证
6. 回写 `state.md`

## 关键约束

- 先找根因，后修复
- 修复过程不能只留在对话里
