# Handoff 模板

## 基础信息

- 创建时间：2026-04-16
- mission：`skill-devflow-optimization`
- 当前阶段：Checkpoint / Handoff
- handoff 编号：002
- 是否 superseded：否

## 当前目标

- 让 `devflow` 作为新的长期开发主入口稳定成型，并在完成一轮边界收口后，开始进入真实场景 smoke test 与最小 eval。

## 当前进度

- `devflow` 设计、第一版实现与第二轮边界优化都已完成。
- `superpowers-*` 已补到接近原版强度，并补齐了相关资源文件。
- `openspec-*` 已按生命周期控制器定位完成一轮收口。
- `devflow` 外层已经回到更清晰的“调度 / 记录 / 恢复”定位。
- `devflow` 内置 `session-handoff` 已对齐为 checkpoint-first 恢复顺序。
- 当前还没有做真实任务 smoke test，也没有做最小 trigger / workflow eval。

## 本轮完成内容

- [x] 回读原始 `devflow` 定位，确认核心分工仍应保持为：
  - OpenSpec 管阶段推进
  - Superpowers 管思考质量
  - DevFlow 管主线记录、路径调度与恢复
- [x] 将 `superpowers-*` 子技能补到接近原版强度
- [x] 补齐相关 prompt / reference / helper 文件
- [x] 将 `openspec-explore`、`openspec-propose`、`openspec-apply-change`、`openspec-archive-change` 收口为更纯的生命周期控制器
- [x] 收敛 `devflow` 外层职责，减少与子技能的重复说明
- [x] 将 `devflow` 内置 `session-handoff` 恢复顺序改为：
  - 先读 `state.md`
  - 再读最近 `checkpoint`
  - handoff 作为补充恢复入口
- [x] 更新 `.explore/skill-devflow-optimization/` 任务记录
- [x] 更新 `NEXT-SESSION-PROMPT.md`

## 关键决策与原因

| 决策 | 备选方案 | 原因 |
| --- | --- | --- |
| `superpowers-*` 优先贴近原版，只做最小环境适配 | 继续保留精简版；整套中文重写 | 当前问题主要来自精简过度，先恢复原版强度更稳 |
| `openspec-*` 只优化生命周期，不承担质量方法论 | 继续给 `openspec-*` 加厚反模式和验证哲学 | 原始分工已经明确，混职责会重新变厚变乱 |
| `devflow` 外层开始收敛，不再继续大幅加厚 | 继续把阶段细节写进主 skill | 外层应回到调度器、记录器、恢复器定位 |
| `session-handoff` 恢复顺序改为 checkpoint-first | 保持 handoff-first | 当前 `devflow` 正常收尾常只写 checkpoint，不一定写 handoff，checkpoint-first 更符合实际真相源 |

## 关键文件 / 产物

| 文件 | 作用 | 相关性 |
| --- | --- | --- |
| `.codex/skills/devflow/SKILL.md` | `devflow` 主入口，总纲与职责边界 | 必读 |
| `.codex/skills/devflow/skills/openspec-explore/SKILL.md` | Explore 生命周期控制 | 必读 |
| `.codex/skills/devflow/skills/openspec-propose/SKILL.md` | Propose 生命周期控制 | 必读 |
| `.codex/skills/devflow/skills/openspec-apply-change/SKILL.md` | Apply 生命周期控制 | 必读 |
| `.codex/skills/devflow/skills/openspec-archive-change/SKILL.md` | Archive 生命周期控制 | 必读 |
| `.codex/skills/devflow/skills/session-handoff/SKILL.md` | `devflow` 内置恢复 / 交接规则 | 必读 |
| `docs/superpowers/specs/2026-04-09-devflow-design.md` | 初始设计说明 | 需要回顾整体意图时读取 |
| `docs/superpowers/plans/2026-04-09-devflow-implementation.md` | 实现计划 | 理解原定范围时读取 |
| `NEXT-SESSION-PROMPT.md` | 直接复制即可续接的提示词 | 用户恢复时首选 |

## 风险 / 阻塞项 / 开放问题

- [ ] 还没有做真实任务 smoke test。
- [ ] 还没有做 `skill-creator-cc` 风格的最小 eval。
- [ ] `devflow` 与 `context-budget-explore` / `spark-workflow` 的触发边界还没有通过真实 prompt 验证。
- [ ] 旧 `context-budget-explore` 的迁移策略尚未开始设计。
- [ ] `context-budget-explore` 自己的内置 `session-handoff` 仍沿用旧的 handoff-first 恢复逻辑，本轮未同步优化。

## 立即下一步

1. 不要继续大幅改写 `devflow` 文档，先切到验证模式。
2. 用 `skill-creator-cc` 的思路准备一组最小高价值 eval prompt，至少覆盖：
   - 应该触发 `devflow`
   - 不应该触发 `devflow`
   - `devflow` 与 `context-budget-explore` / `spark-workflow` 的冲突边界
3. 用一个轻量任务、一个重型任务、一个 resume 场景各试跑一次 `devflow`，重点检查：
   - 路径判断
   - Align -> Plan -> Propose / Apply 顺序
   - `state` / `checkpoint` / `handoff` 链路
4. 如果验证结果稳定，再决定：
   - 是否继续优化其它主 skill
   - 是否开始设计旧 skill 的迁移关系

## 恢复指引

1. 先读取 `.explore/skill-devflow-optimization/state.md`
2. 再读取 `.explore/skill-devflow-optimization/checkpoints.md`
3. 然后读取本 handoff
4. 再读取 `.codex/skills/devflow/SKILL.md`
5. 必要时补读：
   - `.codex/skills/devflow/skills/openspec-*.md`
   - `.codex/skills/devflow/skills/session-handoff/SKILL.md`
   - `docs/superpowers/specs/2026-04-09-devflow-design.md`
   - `docs/superpowers/plans/2026-04-09-devflow-implementation.md`
6. 从“立即下一步”的第 1 条开始继续

## 可从活跃上下文移除的内容

- 关于 `devflow` 是否继续加厚、`openspec-*` 是否承担质量方法论、`session-handoff` 是否 checkpoint-first 的争论都已经收口到文件中。
- 这轮的大部分价值已经从“继续讨论”转移到“开始验证”，不需要再重复需求澄清。
