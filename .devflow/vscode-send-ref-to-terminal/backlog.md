# Backlog

## 轻量后续想法

- 在 VS Code 中安装 `vscode-send-ref-to-terminal-0.1.0.vsix`，手动验证快捷键是否把引用发送到当前活动终端（Active Terminal）。
- 正式发布前补充 `repository` 字段和 LICENSE 文件，消除 `vsce` 元数据警告。
- 如果用户反馈默认快捷键冲突，可只保留命令（Command）并让用户自行绑定快捷键（Keybinding）。
